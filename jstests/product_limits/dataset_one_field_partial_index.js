/**
 * Tests the limits of the product for the DatasetOneFieldPartialIndex dataset.
 */

import {DatasetOneFieldPartialIndex} from "jstests/product_limits/libs/datasets.js";

new DatasetOneFieldPartialIndex().runDataset();
