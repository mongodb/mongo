# Default Mongo Version if a version is not specified. This should point at the release version
# that this branch is targeting. The Server Triage and Release (STAR) team will upgrade this
# version on this branch after every release.
common --define=MONGO_VERSION=8.0.28
