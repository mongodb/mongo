# -*- mode: python -*-

Import("env")

env = env.Clone()

env.CppUnitTest(
    target="db_geo_test",
    source=[],
    LIBDEPS=[],
)

env.Benchmark(
    target="hash_bm",
    source=[
        "hash_bm.cpp",
    ],
    LIBDEPS=["geometry"],
)
