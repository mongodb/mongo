# -*- mode: python -*-

Import("env")

env = env.Clone()

env.Library(
    target="optimizer",
    source=[
        "defs.cpp",
        "node.cpp",
    ],
    LIBDEPS=[
#        "$BUILD_DIR/mongo/base",
    ],
)

env.CppUnitTest(
    target='optimizer_test',
    source=[
        'optimizer_test.cpp',
    ],
    LIBDEPS=[
        'optimizer',
    ]
)
