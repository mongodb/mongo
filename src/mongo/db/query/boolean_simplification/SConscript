# -*- mode: python -*-

Import("env")

env.CppUnitTest(
    target="boolean_simplification_test",
    source=[],
    LIBDEPS=[],
)

env.Benchmark(
    target="quine_mccluskey_bm",
    source=[],
    LIBDEPS=[],
    BAZEL_BENCHMARK_TAG="first_half_bm",
)
