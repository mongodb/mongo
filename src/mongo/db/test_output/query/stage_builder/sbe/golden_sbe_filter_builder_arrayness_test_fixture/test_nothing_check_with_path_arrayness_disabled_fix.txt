# Golden test output of GoldenSbeFilterBuilderArraynessTestFixture/TestNothingCheckWithPathArraynessDisabledFix
DisabledFix_EqNull_ArrayWithScalarIntermediate

    let [
        l4.0 = getField(s1, "a") 
    ] 
    in 
        if (typeMatch(l4.0, 24) ?: false) 
        then traverseF(move(l4.0), lambda(l5.0) { 
            if (isObject(l5.0) ?: false) 
            then traverseF(getField(move(l5.0), "b"), lambda(l6.0) { ((
                if (isNull(l6.0) ?: true) 
                then null 
                else move(l6.0) 
            == null) ?: false) }, false) 
            else false 
        }, false) 
        else exists(s1) 

# Golden test output of GoldenSbeFilterBuilderArraynessTestFixture/TestNothingCheckWithPathArraynessDisabledFix
DisabledFix_EqNull_EmptyArrayIntermediate

    let [
        l10.0 = getField(s2, "a") 
    ] 
    in 
        if (typeMatch(l10.0, 24) ?: false) 
        then traverseF(move(l10.0), lambda(l11.0) { 
            if (isObject(l11.0) ?: false) 
            then traverseF(getField(move(l11.0), "b"), lambda(l12.0) { ((
                if (isNull(l12.0) ?: true) 
                then null 
                else move(l12.0) 
            == null) ?: false) }, false) 
            else false 
        }, false) 
        else exists(s2) 

# Golden test output of GoldenSbeFilterBuilderArraynessTestFixture/TestNothingCheckWithPathArraynessDisabledFix
DisabledFix_EqNull_ScalarIntermediate

    let [
        l16.0 = getField(s3, "a") 
    ] 
    in 
        if (typeMatch(l16.0, 24) ?: false) 
        then traverseF(move(l16.0), lambda(l17.0) { 
            if (isObject(l17.0) ?: false) 
            then traverseF(getField(move(l17.0), "b"), lambda(l18.0) { ((
                if (isNull(l18.0) ?: true) 
                then null 
                else move(l18.0) 
            == null) ?: false) }, false) 
            else false 
        }, false) 
        else exists(s3) 

