\* Green configuration: the fix is in place (term + primary index update
\* atomically on the follower side). Safety + liveness must both hold.
\* See TermCurrentPrimary.tla for instructions.

CONSTANT Server = {1, 2, 3}
CONSTANT Nil = 0
CONSTANT AtomicPrimaryUpdate = TRUE
CONSTANT MaxTerm = 3

INVARIANT PrimaryIndexConsistentWithTerm

PROPERTY EveryNodeEventuallyLearnsCurrentPrimary

CONSTRAINT StateConstraint
SPECIFICATION Spec
