\* Canonical (safe) model-check configuration for LCGMajorityLossRecovery.tla.
\* Requires the operator-attestation gate; expects all invariants to hold.

SPECIFICATION Spec

CONSTANTS
    Node = {n1, n2, n3, n4, n5}
    MaxLSN = 3
    MaxGeneration = 2
    RequireAttestation = TRUE

CONSTRAINT StateConstraint

SYMMETRY NodeSymmetry

INVARIANT
    TypeOK
    NoCommittedDataLoss
    MonotonicLSNFence
    QuorumLossWhileLost
