SPECIFICATION Spec

CONSTANTS
    Shards = {s1, s2, s3}
    NameSpaces = {a}

    UNSHARDS = 1

    \* Flip to TRUE to model the SPM-3961 fix (RefreshChunklessShardCSS as part of commit).
    \* Leave FALSE to reproduce SERVER-123346: TLC will produce a counterexample showing the
    \* chunkless shard's CSS UUID diverging from the config server's, and the downstream
    \* dropCollection tassert firing.
    FIX_ENABLED = FALSE

INVARIANTS
    \* -- Spec correctness invariants.
    TypeOK
    \* -- Protocol correctness invariants.
    \* CSSUUIDMatchesConfigServerOrIsCleared       \* expected to fail with FIX_ENABLED = FALSE
    \* DropCollectionNeverTassertsOnCSSMismatch    \* expected to fail with FIX_ENABLED = FALSE
    \* -- Bait invariants. Selectively enable to generate a counterexample trace.
    \* BaitReachedDrop
    \* BaitChunklessExists

CONSTRAINTS
    StateConstraint

SYMMETRY Symmetry
