# -*- mode: python -*-

Import("env", "jsEngine")

if jsEngine:
    env.CppUnitTest(
        target="scripting_mozjs_test",
        source=[],
        LIBDEPS=[],
    )

env.CppUnitTest(
    target="scripting_test",
    source=[],
    LIBDEPS=[],
)
