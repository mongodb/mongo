load("//bazel:mongo_src_rules.bzl", "mongo_cc_library")

package(default_visibility = ["//visibility:public"])

mongo_cc_library(
    name = "windows_sasl",
    deps = select({
        "@platforms//os:windows": ["@windows_sasl//:sasl"],
        "//conditions:default": [],
    }),
)
