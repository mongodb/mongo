# Placeholder for source-tree loads of mongo_toolchain_flags_v5.bzl (e.g. the flags
# unittest in this package). The real file is generated into each toolchain
# repository by the toolchain repository rule (see MONGO_OPENSSL_ROOT in
# mongo_toolchain.bzl), which populates these lists from the environment; they are
# empty for normal builds.

OPENSSL_LINK_DIRS = []

OPENSSL_INCLUDE_DIRS = []
