base_suite: change_streams_sharding_fsm
description: >-
  Runs the change stream sharding FSM tests with an embedded config server
  in database watch mode.
overrides:
  - "change_streams_sharding_fsm.embedded"
  - "change_streams_sharding_fsm.watch_db"
