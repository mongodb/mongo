base_suite: change_streams_sharding_fsm
description: >-
  Runs the change stream sharding FSM tests with batchSize=1 instead of the
  server default batch size.
overrides:
  - "change_streams_sharding_fsm.batch_size_1"
