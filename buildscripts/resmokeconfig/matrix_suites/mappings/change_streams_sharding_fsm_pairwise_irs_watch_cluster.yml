base_suite: change_streams_sharding_fsm
description: >-
  Runs the continuous FSM test comparing strict vs ignoreRemovedShards in cluster watch mode.
overrides:
  - "change_streams_sharding_fsm.pairwise_irs"
  - "change_streams_sharding_fsm.watch_cluster"
