base_suite: change_streams_sharding_fsm
description: >-
  Runs the change stream sharding FSM tests with ignoreRemovedShards in database watch mode.
overrides:
  - "change_streams_sharding_fsm.irs"
  - "change_streams_sharding_fsm.watch_db"
