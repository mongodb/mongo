base_suite: change_streams_sharding_fsm
description: >-
  Runs the change stream sharding FSM tests with a concurrent BackgroundMutator,
  batchSize=1, and an embedded config server (config shard).
overrides:
  - "change_streams_sharding_fsm.bg_mutator"
  - "change_streams_sharding_fsm.batch_size_1"
  - "change_streams_sharding_fsm.embedded"
