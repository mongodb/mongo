logging:
  executor:
    format: '[%(name)s] %(asctime)s %(message)s'
    handlers:
      - class: logging.StreamHandler
  fixture:
    format: '[%(name)s] %(message)s'
    handlers:
      - class: buildlogger
  tests:
    format: '[%(name)s] %(asctime)s %(message)s'
    handlers:
      - class: buildlogger
