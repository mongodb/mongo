logging:
  executor:
    format: '%(asctime)s [%(name)s] %(message)s'
    handlers:
      - class: logging.StreamHandler
  fixture:
    format: '[%(name)s] %(message)s'
    handlers:
      - class: logging.StreamHandler
  tests:
    format: '%(asctime)s [%(name)s] %(message)s'
    handlers:
      - class: logging.StreamHandler
