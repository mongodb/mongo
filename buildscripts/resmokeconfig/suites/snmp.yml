selector:
  js_test:
    roots:
    - src/mongo/db/modules/*/jstests/snmp/*.js

# snmp tests start their own mongod's.
executor:
  js_test:
    config:
      shell_options:
        nodb: ''
        readMode: commands
