test_kind: js_test

selector:
  roots:
    - jstests/replsets/*.js
    - jstests/replsets/**/*.js
    - src/mongo/db/modules/enterprise/jstests/fcbis/*.js
  exclude_files:

executor:
  config:
    shell_options:
      nodb: ""
