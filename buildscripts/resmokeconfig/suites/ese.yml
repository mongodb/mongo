test_kind: js_test

selector:
  roots:
  - src/merizo/db/modules/*/jstests/encryptdb/*.js

# Encrypted storage engine tests start their own merizod processes
executor:
  config:
    shell_options:
      nodb: ''
      readMode: commands
