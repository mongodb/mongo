test_kind: js_test

selector:
  roots:
  - src/merizo/db/modules/*/jstests/sasl/*.js

# sasl tests start their own merizod's.
executor:
  config:
    shell_options:
      nodb: ''
      readMode: commands
