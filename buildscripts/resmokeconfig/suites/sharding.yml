test_kind: js_test

selector:
  roots:
  - jstests/sharding/*.js
  - jstests/sharding/health_monitor/*.js
  exclude_files:
  - jstests/sharding/mrShardedOutput.js

executor:
  config:
    shell_options:
      nodb: ''
      readMode: commands
