test_kind: js_test

selector:
  roots:
  - jstests/sharding/*.js
  exclude_files:
  - jstests/sharding/kill_sessions.js

executor:
  config:
    shell_options:
      nodb: ''
      readMode: commands
