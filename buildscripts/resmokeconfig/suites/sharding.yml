selector:
  js_test:
    roots:
    - jstests/sharding/*.js
    - jstests/sharding/replset_config/*.js
    exclude_files:

executor:
  js_test:
    config:
      shell_options:
        nodb: ''
        readMode: commands
