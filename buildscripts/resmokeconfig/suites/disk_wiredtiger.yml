test_kind: js_test

selector:
  roots:
  - jstests/disk/*.js

executor:
  config:
    shell_options:
      nodb: ''
      global_vars:
        TestData:
          storageEngine: wiredTiger
      readMode: commands
