selector:
  js_test:
    roots:
    - jstests/core/*.js

executor:
  js_test:
    config:
      shell_options:
        rpcProtocols: opCommandOnly
    hooks:
    - class: CleanEveryN
      n: 20
    fixture:
      class: MongoDFixture
      mongod_options:
        nopreallocj: ''
        set_parameters:
          enableTestCommands: 1
