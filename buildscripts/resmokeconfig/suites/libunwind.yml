test_kind: js_test

selector:
  from_target: //jstests/suites/programmability:libunwind

executor:
  config: {}
  fixture:
    class: MongoDFixture
    mongod_options:
      set_parameters:
        enableTestCommands: 1
