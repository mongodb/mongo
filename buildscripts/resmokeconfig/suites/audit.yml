test_kind: js_test

selector:
  roots:
  - src/mongo/db/modules/*/jstests/audit/*.js

# audit tests start their own mongod's.
executor:
  config:
    shell_options:
      nodb: ''
      readMode: commands
