test_kind: js_test

selector:
  roots:
    - jstests/with_mongot/search_mocked/*.js
    - jstests/with_mongot/mongotmock/*.js
  exclude_with_any_tags:
    - requires_auth

executor:
  config:
    shell_options:
      nodb: ""
