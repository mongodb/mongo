selector:
  js_test:
    roots:
    - jstests/sharding/*.js
    - jstests/sharding/sync_cluster_config/*.js
    exclude_files:
    # Tests shard upgrade to CSRS mode, nonsensical if using SCCC config servers
    - jstests/sharding/csrs_upgrade_mongod_using_movechunk.js

executor:
  js_test:
    config:
      shell_options:
        global_vars:
          TestData:
            useLegacyConfigServers: true
        nodb: ''
        readMode: commands
