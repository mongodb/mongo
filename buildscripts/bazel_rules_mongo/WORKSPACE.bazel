# Python dependencies used to be sourced from a local poetry.lock via
# rules_poetry. Under the Poetry -> uv migration, the outer workspace's
# uv.lock is now the single source of truth; the outer //WORKSPACE.bazel
# loads bazel_rules_mongo as a `local_repository` with
# `repo_mapping = {"@poetry": "@pypi"}` so BUILD.bazel files here can keep
# referencing `@poetry//:<pkg>` (via the shim in //bazel/uv:defs.bzl within
# this sub-workspace) and land in the outer pycross-generated hub.

load("//codeowners:codeowners_validator.bzl", "codeowners_validator")

codeowners_validator()

load("//codeowners:codeowners_binary.bzl", "codeowners_binary")

codeowners_binary()
